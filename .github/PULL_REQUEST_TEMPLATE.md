## Summary / 概要

## Verification / 検証

```sh
npm run check
```

- [ ] `npm run check` passes (or state exactly what was run instead)
- [ ] Tests cover the change and its failure paths
- [ ] Trust boundaries preserved (closed schemas, size limits, hash checks, no tracking)
- [ ] No credentials, private data, local model files or machine-specific paths added

Strength-affecting change? State the comparison conditions (opponents, clocks,
game counts). 棋力に影響する変更は比較条件を明記してください。
